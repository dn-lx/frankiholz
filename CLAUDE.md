# Claude Code Bootstrap

`AGENTS.md` is the canonical project instruction file.

Before editing:
1. Read `AGENTS.md`.
2. Read `docs/PROJECT-MEMORY.md`.
3. Read `docs/CURRENT-HANDOFF.md`.
4. Read `docs/AGENT-PLATFORM-WORKFLOWS.md`.
5. If external systems are needed, read `docs/MCP-SETUP.md` and verify the required MCP connections.

Do not duplicate or override shared project rules here. Feature/fix/chore work starts from `develop` and merges back to `develop`; never merge directly to `main`.
